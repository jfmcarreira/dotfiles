
#- alias: Notify Bianca Home
  #trigger:
  #- platform: state
    #entity_id: group.bianca
    #to: home
  #action:
  #- service: notify.notify_all
    #data_template:
      #message: >
        #{% if is_state('group.bianca', 'on') %}
          #Bianca is home
        #{% else %}
          #Bianca is away
        #{% endif %}
