##################################################
# Alias
##################################################
alias reload='source ~/.zshrc'
alias updatedotfiles='git -C ~/.dotfiles pull && git -C ~/.dotfiles submodule foreach git pull && source ~/.zshrc'

alias grep='grep --colour'
alias egrep='egrep --colour'
alias fgrep='fgrep --colour'
